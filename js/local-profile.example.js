// ============================================
// LOCAL PROFILE OVERRIDES (template)
// ============================================
// Copy this file to js/local-profile.js (which is gitignored) and fill in
// your personal values. Every key here overrides the default in the App's
// useState — any param from js/app-main.js works. This keeps personal
// financial data out of the public repository.
//
// cp js/local-profile.example.js js/local-profile.js

const LOCAL_PROFILE = {
    // initialPortfolioUSD: 500000,
    // initialPortfolioBRL: 1000000,
    // currentAge: 40,
    // years: 60,
    // inssMonthlyBRL: 3000,
    // minimumWithdrawalBRL: 120000,
    // monthlyContributionBRL: 10000,
    // targetSpendingBRL: 180000,
    // contributionSplitUSD: 80,
};
